# EXPREZZZO House Docker Environment Variables
# For containerized deployment with docker-compose.escape.yml

# Database Configuration (Docker PostgreSQL)
DATABASE_URL=postgresql://exprezzzo:sovereign@localhost:5433/exprezzzo_house

# Redis Configuration (Docker Redis)
REDIS_URL=redis://localhost:6380

# Ollama Configuration (Docker Ollama)
OLLAMA_BASE_URL=http://localhost:11435

# Next.js Configuration
NEXT_PUBLIC_SUPABASE_URL=http://localhost:3003
NEXTAUTH_SECRET=sovereign-house-secret-key-vegas-style
NEXTAUTH_URL=http://localhost:3003

# Production Deployment Notes:
# 1. Update ports to match your Docker setup
# 2. Set appropriate secrets for production
# 3. Configure Vercel environment variables with production values